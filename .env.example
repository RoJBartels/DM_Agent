# Copy to .env and fill in. All settings are prefixed DM_.
DM_DATABASE_URL=postgresql+asyncpg://dm:dm@localhost:5433/dm_agent
# Optional — the Anthropic SDK also picks up ANTHROPIC_API_KEY or an `ant auth login` profile.
# DM_ANTHROPIC_API_KEY=sk-ant-...
